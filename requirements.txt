# Alice 桌面快捷 AI 辅助工具 —— Python 运行依赖
# 安装：pip install -r requirements.txt
# （按规格要求：未安装时程序只提示名称，不会自动安装）

pywebview>=6.0.0      # 桌面混合框架（无边框 / 置顶 / 不进任务栏的单窗口）
keyboard>=0.13.5      # 全局热键
pystray>=0.19.5       # 系统托盘
Pillow>=10.0.0        # pystray 托盘图标绘制

# ---- 内置 LangGraph 接口（plugins/deepseek、plugins/ModelScope）----
magicalimport~=0.9.2
langchain-core~=1.6.2
langgraph~=1.2.11
langchain-openai~=1.6.0
langchain-deepseek~=1.1.0
pyyaml~=6.0.3

# 内置工具（tools/ 下的 requests / geocoder 在每次对话时都会被导入）
requests>=2.31
geocoder~=1.38.1